You are a senior AI systems architect and backend engineer.

Design a COMPLETE, production-ready implementation plan for a project called:

“FlowForge AI” — an OpenEnv-based environment where an AI agent performs real-world workflow tasks using structured tool interactions and reward-driven feedback.

---

## 🎯 STRICT HACKATHON REQUIREMENTS (MUST FOLLOW EXACTLY)

1. File Structure:
- inference.py MUST be in the ROOT directory
- Proper modular structure (models, tools, tasks, environment, server)

2. Environment API (STRICT SIGNATURES):
- def reset(self, seed=None, episode_id=None, **kwargs)
- def step(self, action, timeout_s=None, **kwargs)

3. Structured Logging (MANDATORY FORMAT in inference.py):
Print EXACTLY:
[START] episode_id=...
[STEP] action=... reward=... score=...
[END] final_score=...

4. Grading System:
- Deterministic scoring ONLY (no randomness)
- Smooth score between 0.0–1.0 (NOT binary)
- Must support partial progress rewards

5. Performance Constraints:
- Must run within 2 vCPU, 8GB RAM
- Total runtime < 20 minutes

6. LLM Usage:
- inference.py MUST use OpenAI client
- LLM used ONLY for decision making (NOT grading)

7. Server Requirement:
- Pass environment class (NOT instance)
  Example: create_app(FlowForgeEnvironment)

8. System Behavior:
- No crashes on invalid actions
- Must handle edge cases (loops, wrong tools, missing params)

---

## 🧠 SYSTEM DESIGN REQUIREMENTS

- Use ReAct loop (reason → act → observe)
- Use structured actions (JSON format, no free text)
- Simulate tools (database, email, file system, query engine)
- Maintain internal state tracking progress

---

## 📦 OUTPUT FORMAT (VERY IMPORTANT)

Provide a COMPLETE implementation plan with:

---

### 1. Project Folder Structure
- Full directory tree
- Exact file names (must match hackathon expectations)

---

### 2. System Architecture
- Clear explanation
- Simple ASCII diagram

---

### 3. Data Models
- Action schema (JSON structure)
- Observation schema
- State structure

---

### 4. Tool Design
- At least 3–5 tools
- Input/output schema
- Validation rules

---

### 5. Environment Design
- State transitions
- Internal tracking variables

---

### 6. step() and reset() Logic
- Pseudocode (STRICTLY matching required signatures)
- Error handling
- Max step limit

---

### 7. Task Design
- 3 tasks (Easy, Medium, Hard)
- Objectives for each task
- Expected workflow

---

### 8. Reward System
- Step rewards
- Final reward
- Penalties
- Formula for smooth scoring

---

### 9. Grader Design
- Deterministic rule-based scoring
- Partial completion handling

---

### 10. inference.py Design (CRITICAL)
- Must include:
  - OpenAI client usage
  - Loop for agent interaction
  - EXACT logging format:
    [START], [STEP], [END]

---

### 11. Edge Case Handling
- Invalid actions
- Infinite loops
- Tool failures

---

### 12. Performance Optimization
- Ensure <20 min runtime
- Efficient data handling

---

## ⚠️ IMPORTANT RULES

- Do NOT give generic answers
- Do NOT skip implementation details
- Everything must be BUILDABLE
- Follow hackathon specs strictly
- Keep it simple and feasible

---

## 🎯 GOAL

The output should allow a developer to directly start coding and PASS OpenEnv validation without modification.
